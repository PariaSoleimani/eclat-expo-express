{
	"name": "eclat-server",
	"version": "1.0.0",
	"main": "server.js",
	"private": true,
	"type": "module",
	"scripts": {
		"start": "NODE_ENV=production node server",
		"dev": "nodemon server",
		"lint": "biome check .",
		"format": "biome format --write ."
	},
	"dependencies": {
		"express": "^5.2.1",
		"pg": "^8.22.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.6",
		"dotenv": "^17.4.2",
		"nodemon": "^3.1.14"
	}
}
